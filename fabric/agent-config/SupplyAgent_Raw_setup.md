# SupplyAgent_Raw — setup

The Raw agent is the deliberately-unhelped baseline. Its failures are the demo.

1. Create a data agent named **SupplyAgent_Raw** in the Option B folder.
2. Add ONE data source: the **MultiSource_Raw** semantic model.
3. Add **no instructions**. No Prep for AI on MultiSource_Raw, no agent instructions.
4. Save. That's it.

Do not "help" it. The contrast only holds if v1 is genuinely bare.
